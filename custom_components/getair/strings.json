{
  "config": {
    "step": {
      "user": {
        "title": "Connect to getAir",
        "description": "Enter your getAir account credentials",
        "data": {
          "username": "Email / Username",
          "password": "Password"
        }
      }
    },
    "error": {
      "invalid_auth": "Invalid username or password"
    },
    "abort": {
      "already_configured": "Account already configured"
    }
  }
}
